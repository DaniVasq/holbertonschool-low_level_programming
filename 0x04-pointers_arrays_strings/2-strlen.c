#include "holberton.h"

/**
 * _strlen - returns the length of the string.
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; *(s + i) != '\0'; i++)
	{
	}
	return (i);
}
