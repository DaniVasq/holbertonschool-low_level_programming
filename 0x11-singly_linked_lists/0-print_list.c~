#include "lists.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * print_list - prints all the elements of a list_t list.
 *
 * Return: .
 */
size_t print_list(const list_t *h)
{
	size_t i = 0;

	for (i = 0; h != NULL; i++)
	{
		h = h->next;
	}
	if (str == NULL)
	{
		printf("[0] (nil)");
	}
	return (i);
}
