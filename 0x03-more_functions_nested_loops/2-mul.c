/**
 * mul - multiplies two integers.
 *@a: integer one
 *@b: integer two
 * Return: result of multiplying two integers.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
